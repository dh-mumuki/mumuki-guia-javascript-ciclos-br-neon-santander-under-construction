describe("Calcular somatoria(4)", function() {
  it("deveria devolver 6", function() {
	assert.equal(somatoria(4), 6)
  });
})
describe("Calcular somatoria(5)", function() {
  it("deveria devolver 10", function() {
	assert.equal(somatoria(5), 10)
  });
})
